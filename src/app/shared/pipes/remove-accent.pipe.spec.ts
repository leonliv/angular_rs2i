import { RemoveAccentPipe } from './remove-accent.pipe';

describe('RemoveAccentPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveAccentPipe();
    expect(pipe).toBeTruthy();
  });
});
